{
  "name": "mira",
  "description": "Bridges chat from the Mira iOS app into Claude Code",
  "mcpServers": {
    "mira": {
      "command": "sh",
      "args": [
        "-lc",
        "cd \"${CLAUDE_PLUGIN_ROOT}\" && { [ -d node_modules ] || bun install --silent >>/tmp/mira.log 2>&1; }; exec bun \"${CLAUDE_PLUGIN_ROOT}/server.ts\" 2>>/tmp/mira.log"
      ]
    }
  }
}
